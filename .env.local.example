# =============================================================================
# Zomate Fitness — Next.js（UI）
# 資料一律經 FastAPI（zomate-fitness-system-back）存 PostgreSQL（zomate_fs_*），
# 例如在 Render eventxp 取得 External Database URL 後設於「後端」的 DATABASE_URL。
# =============================================================================

# 選項 A — 連已部署的 FastAPI（推薦預覽／試機）
# NEXT_PUBLIC_API_BASE_URL=https://your-api.onrender.com

# 選項 B — 本機同時跑 uvicorn（預設開發模式可不設：會自動用 http://127.0.0.1:8000）
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# 僅當你需要 Next Route Handler mock（離線／無後端）時設為 1（預設不要設）
# NEXT_PUBLIC_USE_NEXT_MOCK_API=1
